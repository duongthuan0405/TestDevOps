pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Building the project..."'
                sh 'echo "Build completed."'
            }
        }
    }
}